Algoritmo "Mostrar valores pares"

Var

   valor, cont: inteiro

Inicio

      Escreva("Digite um valor: ")
      Leia(valor)
      
      Se(valor > 0) entao
         Para cont <- 0 ate valor passo 2 faca
              Escreval(cont)
         FimPara
      Senao
         Para cont <- 0 ate valor passo -2 faca
              Escreval(cont)
         FimPara
      FimSe

Fimalgoritmo